name: foundry
version: 0.3.0
config-version: 2

profile: foundry

model-paths: ["models"]
seed-paths: ["seeds", "/var/lib/foundry/dbt/seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

on-run-start:
  - "INSTALL vss"
  - "LOAD vss"
  - "SET hnsw_enable_experimental_persistence = true"

models:
  foundry:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table

seeds:
  foundry:
    +schema: silver
    dim_category_overrides:
      +column_types:
        stable_id: varchar
        category_id: varchar
        notes: varchar
    dim_transfer_rules:
      +column_types:
        rule_id: varchar
        pattern: varchar
        direction: varchar
        notes: varchar
